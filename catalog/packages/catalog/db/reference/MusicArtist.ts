export interface MusicArtist {
  name: string;
  role: string; // "composer", "director", "singer", etc...
  vendorArtistId: string;
  vendorName: string;
  rank: string;
  sortKey: number;
}
